stages:
  - hammer_pkg

make_hammer_pkg:
    stage: hammer_pkg
    image: registry.aereldesign.net/aerelcommon/tools/hammer/minimal:latest
    tags:
        - docker
    script:
        - hammer "ci::publish"
    rules:
        - if: $CI_COMMIT_TAG
